{
  "name": "@supabase/stripe-sync-cli",
  "version": "0.0.0",
  "description": "CLI to sync your stripe data to postgres",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "stripe-sync": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "dev": "tsx src/index.ts",
    "lint": "eslint src",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "stripe:trigger": "./scripts/stripe-trigger.sh",
    "verify-webhook-reuse": "tsx scripts/verify-webhook-reuse.ts",
    "cleanup-webhooks": "tsx scripts/cleanup-webhooks.ts",
    "test:integration": "./scripts/test-integration.sh",
    "test:integration:backfill": "./scripts/test-integration-backfill.sh"
  },
  "keywords": [
    "stripe",
    "webhook",
    "cli",
    "development",
    "sync"
  ],
  "dependencies": {
    "@supabase/stripe-sync-fastify": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "dotenv": "^16.4.7",
    "inquirer": "^12.3.0",
    "pg": "^8.13.1",
    "stripe": "^17.7.0",
    "stripe-replit-sync": "workspace:*"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.15.6",
    "tsx": "^4.19.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
